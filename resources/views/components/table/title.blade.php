<div class="flex items-center gap-3">
    <span class="bg-gray-700 w-3 h-3 rounded-full"></span>
    <h1 class="poppins-semibold text-2xl">{{ $title }}</h1>

    <a href="{{ $link }}"class="border border-dashed border-gray-700 
        px-3 flex items-center inline-block inter text-lg items-center rounded-full">
        + Baru
    </a>
</div>

<!-- <div class="flex items-center gap-3">
    <span class="bg-gray-700 w-3 h-3 rounded-full"></span>
    <h1 class="poppins-semibold text-2xl">{{ $title }}</h1>

    @if (!empty($link))
        <a href="{{ $link }}" class="border border-dashed border-gray-700 
            px-3 flex items-center inline-block inter text-lg rounded-full">
            + Baru
        </a>
    @endif
</div> -->